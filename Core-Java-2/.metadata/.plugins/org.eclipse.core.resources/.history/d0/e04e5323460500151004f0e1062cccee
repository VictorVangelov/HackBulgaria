package HddCrawler;

import java.nio.file.Path;

import org.apache.commons.io.FilenameUtils;

public class crawler {
	
	private int isVideoFormat(String pathToFile){
		
		if (FileFormats.videoFormats.contains(pathToFile.substring(pathToFile.lastIndexOf('.',pathToFile.length())))){
			
		}
			return F
	}

}
